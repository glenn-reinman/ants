// graphObject.js

class GraphObject {
    constructor(img, startX, startY, startDir, depth) {
        this.img = imgs[img];
        this.x = startX;
        this.y = startY;
        this.dir = startDir;
        this.depth = depth;
    }
}
