// graphObject.js

class GraphObject {
    constructor(startX, startY, startDir, img, depth) {
        this.x = startX;
        this.y = startY;
        this.dir = startDir;
        this.img = img;
        this.depth = depth;
    }
}